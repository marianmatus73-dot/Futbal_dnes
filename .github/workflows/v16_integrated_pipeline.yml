name: V16 INTEGRATED AUTONOMOUS CYCLE

on:
  workflow_dispatch:

jobs:
  integrated-cycle:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - run: python v16_integrated_pipeline.py
