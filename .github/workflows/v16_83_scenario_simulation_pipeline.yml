name: V16.83 SCENARIO SIMULATION PIPELINE

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - run: python v16_83_scenario_simulation_pipeline.py