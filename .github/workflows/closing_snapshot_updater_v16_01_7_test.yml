name: V16.01.7 Closing Snapshot Update Test

on:
  workflow_dispatch:

jobs:
  closing-update-test:
    name: Run Closing Snapshot Update Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run closing update test
        run: |
          python closing_snapshot_updater_v16_01_7.py
